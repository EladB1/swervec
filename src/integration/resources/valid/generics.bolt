fn first(Array<generic> array): generic {
    return array[0];
}

fn last(Array<generic> array): generic {
    generic end = array[2];
    return end;
}

Array<float> floats = {-9.8, 3.14, 6.67 * 10 ** -11};
first(floats);
last(floats);
