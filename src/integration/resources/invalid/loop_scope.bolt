fn main(int argc, Array<string> argv): int {
    int x = 5;
    while(x < 10) {
        int y = x - 1;
        if (y % 2 == 0)
            y *= 4;
        else
            y -= 3;
        x++;
    }
    x = y ** 3;
    if (x % 2 != 0)
        --x;
    return 0;
}