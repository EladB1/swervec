fn multiply(int x, double y): double {
    return x * y;
}


const arr = {1,2,3} // this is a semantic error

/" This
is a multiline
string"/

// main is void so no need to annotate it with the return type
fn main() {
    const double pi = 3.14;
    double y;
    for (int x : 0..5) { // 0..5 is seen as an invalid number since .. operator not in language
        y = multiply(x, pi);
    }
}