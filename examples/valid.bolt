string str = "someValue"
int someInteger = 10
const float PI = 3.14
string todo = /"
    - write some code 
    - test code
"/


const array[string] langs = {"C", "Java", "Python", "Typescript"} // 
/*
    An array declared with const is a constant reference to an immutable array by default
    A const mut array is a constant reference to a mutable array 
    mut allows your array to change but the variable that stores the array cannot be reassigned

    non-const arrays are mutable by default so mut is unnecessary
*/
const mut array[int] primes = {2, 19}

fn concat(string first, string second) : string {
    return first + second
}

fn main() {
    print(someValue)
    float y = someInteger * PI + 1
    print(y++)
    someValue
}